# from agent.behavior import train_time_series_forecaster
# from sktime.datasets import load_airline

# def test_forecaster():
#     y = load_airline()
#     model = train_time_series_forecaster(y)
#     preds = model.predict([len(y), len(y)+1, len(y)+2])
#     assert len(preds) == 3
